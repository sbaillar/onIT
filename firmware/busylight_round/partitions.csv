# 16MB flash layout for the Waveshare ESP32-S3-Touch-LCD-1.28, identical to
# the AMOLED board's table. Picked up by arduino-cli via
# PartitionScheme=custom + FlashSize=16M in the FQBN (see Makefile).
# "spiffs" is the label LittleFS.begin() mounts by default; the firmware
# formats it as LittleFS and stores the emoji deck there (/deck/N.rgb).
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x640000,
app1,     app,  ota_1,   0x650000,0x640000,
spiffs,   data, spiffs,  0xC90000,0x350000,
coredump, data, coredump,0xFE0000,0x10000,
